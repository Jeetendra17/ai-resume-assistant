# Core -- everything the site needs to run.
Flask==3.1.0
gunicorn==23.0.0
python-dotenv==1.0.1

# The interview assistant's orchestration: LCEL chains and a LangGraph state
# machine. Measured at ~38 MB unzipped with dependencies -- comfortably inside a
# free serverless bundle. Both are optional at runtime: if they fail to install or
# import, interview/compat provides stdlib stand-ins with the same behaviour, and
# the answer trace reports which engine ran.
langchain-core>=1.6,<2
langgraph>=1.2,<2

# LLM providers are called over plain HTTP (stdlib urllib), so Groq, Gemini,
# OpenRouter, Cerebras, Mistral, Together, Ollama and OpenAI need no extra packages.
# Uncomment only if you want to use Anthropic Claude, which uses its official SDK:
# anthropic>=0.69.0

# Build-time tools (not needed to run the site):
#   reportlab -- build_resume.py and interview/corpus/export_pdf.py
#   numpy     -- interview/index/build_index.py
#   see interview/finetune/requirements-finetune.txt for LoRA training
