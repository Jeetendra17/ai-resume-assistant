# Only for interview/finetune/train_lora.py, on a GPU machine (a free Colab T4 works).
# The website never installs these -- it does not serve a tuned model.
torch>=2.3
transformers>=4.45
peft>=0.13
datasets>=3.0
accelerate>=1.0
